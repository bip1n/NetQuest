import "@/styles/globals.css";


export default function ProfileSetting({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return (
        <section>
        {children}
    </section>
    );
  }
  